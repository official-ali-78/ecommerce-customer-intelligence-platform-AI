# Demo sample data

Small synthetic CSV files (~100 KB total) shipped with the repo so the app runs after `git clone` without downloading Olist.

When you add real files to `dataset/`, the app automatically switches to the full Olist dataset.

Regenerate (optional):

```bash
python scripts/build_sample_data.py
```
